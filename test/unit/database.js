const app = require('../../app');



// describe('Database service', () => {
// 	describe('All tables exist', () => {
// 		it('the users table exists', async () => {
// 			const response = await db.query("SHOW TABLES");
// 			expect(response).toHave('users');
// 		})
// 	});
// });